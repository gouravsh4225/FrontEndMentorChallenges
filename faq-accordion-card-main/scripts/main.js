const questionListElement  = document.querySelectorAll('.question-item');
questionListElement.forEach((question)=>{
    question.addEventListener('click',(item)=>{
        if(item.parentNode.classList.contains('d-none')) {
            
        }
    })
})